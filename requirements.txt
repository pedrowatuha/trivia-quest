# Runtime — the trivia web app
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.6
requests>=2.31

# Model inference
torch>=2.2
transformers>=4.44
peft>=0.11
huggingface_hub>=0.24
accelerate>=0.30

# Training / pipeline (optional — only needed if you re-run pipeline/ or inference/)
# datasets>=2.20
# trl>=0.9
# bitsandbytes>=0.43
